kind = "web"
previewPath = "/"
title = "Kirana Store Manager"
version = "1.0.0"
id = "artifacts/kirana-store"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18732

[services.development]
run = "pnpm --filter @workspace/kirana-store run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/kirana-store", "run", "build" ]
publicDir = "artifacts/kirana-store/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18732"
BASE_PATH = "/"
